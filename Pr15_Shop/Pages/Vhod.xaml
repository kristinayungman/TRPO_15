<Page x:Class="Pr15_Shop.Pages.Vhod"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:Pr15_Shop.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Vhod">

    <!--<materialDesign:DialogHost x:Name="ManagerDialog">-->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
               Text="Выберите роль для входа" FontSize="50"
               Foreground="{DynamicResource MaterialDesign.Brush.Primary}"
               HorizontalAlignment="Center"/>
            <StackPanel Grid.Row="1"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Orientation="Horizontal">
                <Button x:Name="BtnUser"
                Content="Войти как&#10;пользователь"
                Width="180" Height="150"
                FontSize="16"
                Click="BtnUser_Click"
                Margin="5">
                </Button>
                <StackPanel Orientation="Vertical">
                <TextBox x:Name="TextPass"  materialDesign:HintAssist.Hint="Введите пароль"/>
                <Button x:Name="BtnManager"
                     Content="Войти как&#10;менеджер"
                     Width="180" Height="100"
                     FontSize="16"
                     Click="BtnManager_Click"
                     Margin="5">
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
        <!--<materialDesign:DialogHost.DialogContent>
            <StackPanel Margin="24" MinWidth="300">
                <TextBlock Text="Введите пароль менеджера" 
                       Margin="0,0,0,16"/>
                <PasswordBox x:Name="ManagerPasswordBox"
                         materialDesign:HintAssist.Hint="Пароль"
                         Margin="0,0,0,20"
                         KeyDown="ManagerPasswordBox_KeyDown"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Margin="5" Content="Войти" Click="ManagerClick"/>
                    <Button Margin="5" Content="Отмена" Click="Cansle_Dialoge"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:DialogHost.DialogContent>
    </materialDesign:DialogHost>-->
</Page>
