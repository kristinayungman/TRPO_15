<Page x:Class="Pr15_Shop.Pages.ManagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:Pr15_Shop.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ManagerPage"
      Background="AliceBlue">

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Label>Поиск</Label>
            <TextBox Text="{Binding searchQuery, UpdateSourceTrigger=PropertyChanged}" TextChanged="TextBox_TextChanged" materialDesign:HintAssist.Hint="Введите текст для поиска"/>
            <Button Click="Add_Prod" Margin="10">Добавить товар</Button>
            <Button Click="Delete_Prod" Margin="10">Удалить товар</Button>
            <Button Click="Edit" Margin="10">Категории/Бренды/Теги/</Button>
            <Button Content="Назад" Click="Button_Click"></Button>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <Label FontSize="16">Сортировка</Label>
            <ComboBox SelectionChanged="ComboBox_SelectionChanged">
                <ComboBox.Items>
                    <ComboBoxItem Tag="Name">Наименование</ComboBoxItem>
                    <ComboBoxItem Tag="PriceUp">Цена по возрастанию</ComboBoxItem>
                    <ComboBoxItem Tag="Pricedown">Цена по убыванию</ComboBoxItem>
                    <ComboBoxItem Tag="StockUp">Количество по возрастанию</ComboBoxItem>
                    <ComboBoxItem Tag="StockDown">Количество по убыванию</ComboBoxItem>
                </ComboBox.Items>
            </ComboBox>
            <Label>Фильтры</Label>
            <StackPanel>
                <Label FontSize="16">Фильтр по цене</Label>
                <Label>от</Label>
                <TextBox Text="{Binding filterPriceFrom, UpdateSourceTrigger=PropertyChanged}" 
             materialDesign:HintAssist.Hint="Введите цену" 
             TextChanged="TextBox_TextChanged"/>
                <Label>до</Label>
                <TextBox Text="{Binding filterPriceTo, UpdateSourceTrigger=PropertyChanged}" 
             materialDesign:HintAssist.Hint="Введите цену" 
             TextChanged="TextBox_TextChanged"/>
            </StackPanel>
            <StackPanel Margin="0,10,0,0">
                <Label FontSize="16">Фильтр по бренду</Label>
                <ComboBox x:Name="BrandFilterComboBox" 
              SelectionChanged="BrandFilter"
              DisplayMemberPath="Name">
                    <ComboBox.Items>
                        <ComboBoxItem Content="Все бренды" Tag="0"/>
                    </ComboBox.Items>
                </ComboBox>
            </StackPanel>
            <StackPanel Margin="0,10,0,0">
                <Label FontSize="16">Фильтр по категории</Label>
                <ComboBox x:Name="CategoryFilterComboBox" 
              SelectionChanged="CategoryFilter"
              DisplayMemberPath="Name">
                    <ComboBox.Items>
                        <ComboBoxItem Content="Все категории" Tag="0"/>
                    </ComboBox.Items>
                </ComboBox>
            </StackPanel>
            <Button Click="Clear" Margin="5">Сбросить</Button>
        </StackPanel>
        <materialDesign:ColorZone Grid.Row="1" Grid.Column="1" Mode="SecondaryMid">
            <ListView MouseDoubleClick="Edit_Prod" ItemsSource="{Binding products}" Name="FormsList" HorizontalContentAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="8" Margin="4" BorderThickness="1">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Stock}" Value="0"><Setter Property="BorderBrush" Value="#ffd129"/></DataTrigger>
                                        <DataTrigger Binding="{Binding Stock}" Value="1"><Setter Property="BorderBrush" Value="#ffd129"/></DataTrigger>
                                        <DataTrigger Binding="{Binding Stock}" Value="2"><Setter Property="BorderBrush" Value="#ffd129"/></DataTrigger>
                                        <DataTrigger Binding="{Binding Stock}" Value="3"><Setter Property="BorderBrush" Value="#ffd129"/></DataTrigger>
                                        <DataTrigger Binding="{Binding Stock}" Value="4"><Setter Property="BorderBrush" Value="#ffd129"/></DataTrigger>
                                        <DataTrigger Binding="{Binding Stock}" Value="5"><Setter Property="BorderBrush" Value="#ffd129"/></DataTrigger>
                                        <DataTrigger Binding="{Binding Stock}" Value="6"><Setter Property="BorderBrush" Value="#ffd129"/></DataTrigger>
                                        <DataTrigger Binding="{Binding Stock}" Value="7"><Setter Property="BorderBrush" Value="#ffd129"/></DataTrigger>
                                        <DataTrigger Binding="{Binding Stock}" Value="8"><Setter Property="BorderBrush" Value="#ffd129"/></DataTrigger>
                                        <DataTrigger Binding="{Binding Stock}" Value="9"><Setter Property="BorderBrush" Value="#ffd129"/></DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock>
                            <Run Text="★" Foreground="Gold" FontWeight="Bold"/>
                            <Run Text=" Рейтинг: "/>
                                    <Run Text="{Binding Rating, StringFormat={}{0:F1}}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Category.Name}" TextWrapping="Wrap" MaxHeight="40"/>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" MaxHeight="40"/>
                                    <TextBlock Text="{Binding DisplayTags}"  FontWeight="Bold" Margin="0,5,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                    <TextBlock>
                            <Run Text="Цена: "/>
                            <Run Text="{Binding Price, StringFormat={}{0:N2}}"/>
                                    </TextBlock>
                                    <TextBlock>
                            <Run Text="Остаток: "/>
                            <Run Text="{Binding Stock}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </materialDesign:ColorZone>
        <WrapPanel Grid.Row="2" Grid.ColumnSpan="2">
            <TextBlock Text="{Binding products.Count, StringFormat='{}Всего записей: {0}'}"/>
            <TextBlock Text="{Binding Items.Count, ElementName=FormsList, StringFormat='{} Показано: {0}'}"/>
        </WrapPanel>
    </Grid>
</Page>

